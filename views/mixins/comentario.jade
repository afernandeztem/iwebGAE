mixin comentario(comentario)
  .material-card
    img.card-img-top(src=serie.imagen, alt='Card image cap',height='200', width='400')
    .card-body
      h5.card-title #{comentario.titulo}
      p.card-text #{comentario.contenido}
